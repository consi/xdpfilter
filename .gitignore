# bpf2go artifacts — regenerated by `go generate` (run via `make build`/`make dev`).
# A fresh clone must `make build` (or `go generate ./...`) before a bare `go build`.
internal/dataplane/filter_bpfel.go
internal/dataplane/filter_bpfeb.go
internal/dataplane/filter_bpfel.o
internal/dataplane/filter_bpfeb.o

# build output
/dist/
*.deb

# editor / OS cruft
.DS_Store
*.swp
*~
